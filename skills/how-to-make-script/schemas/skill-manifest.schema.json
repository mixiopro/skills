{
  "type": "object",
  "required": [
    "id",
    "name",
    "description",
    "entrypoint",
    "supports",
    "protocol_id",
    "rubric_ids",
    "linked_atoms",
    "fallback_behavior"
  ],
  "properties": {
    "id": {"type": "string"},
    "name": {"type": "string"},
    "description": {"type": "string"},
    "entrypoint": {"type": "string"},
    "surface": {"enum": ["public", "internal"]},
    "supports": {
      "type": "object",
      "required": ["intents", "mediums", "stages", "outputs"],
      "properties": {
        "intents": {"type": "array", "minItems": 1, "items": {"type": "string"}},
        "mediums": {"type": "array", "minItems": 1, "items": {"type": "string"}},
        "stages": {"type": "array", "minItems": 1, "items": {"type": "string"}},
        "outputs": {"type": "array", "minItems": 1, "items": {"type": "string"}}
      }
    },
    "protocol_id": {"type": "string"},
    "rubric_ids": {"type": "array", "minItems": 1, "items": {"type": "string"}},
    "linked_atoms": {"type": "array", "minItems": 1, "items": {"type": "string"}},
    "fallback_behavior": {"type": "string"},
    "default_loading_mode": {"type": "string", "enum": ["route_kernel", "focus_pack", "compare_pack", "teaching_pack", "survey_pack"]},
    "adjunct_bundles": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["trigger"],
        "properties": {
          "trigger": {"type": "string"},
          "mode": {"type": "string", "enum": ["smallest_adjunct", "full_adjunct"]}
        }
      }
    },
    "fallback_to_skill_id": {"type": "string", "nullable": true}
  }
}
