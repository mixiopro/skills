{
  "mcpServers": {
    "mixio": {
      "command": "npx",
      "args": [
        "--yes",
        "--allow-scripts=better-sqlite3",
        "@mixio-pro/mcp@0.6.0"
      ],
      "env": {
        "MIXIO_BASE_URL": "${user_config.mixio_base_url}"
      }
    }
  }
}
