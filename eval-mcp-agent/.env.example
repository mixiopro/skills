# Base URL for the running OpenResponses Hono Orchestrator
EVAL_API_BASE_URL=https://eval-mastra.staging.mixio.pro

# Auth API Key / Token configured on the server
EVAL_API_KEY=your-eval-api-token-here
